# Web UI build-time configuration template.
# Copy to .env and adjust before building: cp .env.example .env && npm run build

# Control Plane API token (must match KURRENT_CP_TOKEN in control_plane.env).
# Leave empty to skip the Authorization header.
# The token is embedded at build time — restart the container after rebuilding.
VITE_CP_TOKEN=
